<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>To-Do List</title>
</head>
<body>
    <h1>To-Do List</h1>
    <ul>
        <% for (const item of todoItems) { %>
            <li><%= item.text %> - <%= item.date %></li>
        <% } %>
    </ul>
    <form action="/user/addTodo" method="POST">
        <input type="text" name="todoText" placeholder="Enter a new ToDo item" required>
        <button type="submit">Add</button>
    </form>
</body>
</html>
